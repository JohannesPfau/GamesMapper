# Core dependencies for the Games Mapper pipeline (src/main.py).
# Tested with Python >= 3.10.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
networkx>=3.0
plotly>=5.18
kaleido>=0.2.1   # only required to export the Mapper graph to PDF (save_as_pdf=True)
